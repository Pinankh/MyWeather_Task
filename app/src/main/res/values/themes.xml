<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyWeather" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!--<item name="colorPrimary">@color/white</item>
        <item name="backgroundColor">@color/white</item>-->
    </style>

    <style name="Theme.MyWeather" parent="Base.Theme.MyWeather" />

    <style name="Theme.MyWeather.Fullscreen" parent="Theme.MyWeather">
        <item name="android:actionBarStyle">@style/Widget.Theme.MyWeather.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.MyWeather.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>